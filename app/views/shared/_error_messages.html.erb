<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t("shared.error_messages.the_form_contains") %>
      <% if I18n.locale == :en %>
        <%= pluralize @user.errors.count, t("shared.error_messages.error") %>.
      <% else %>
        <%= t("shared.error_messages.error", count: @user.errors.count) %>
      <% end %>
    </div>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
